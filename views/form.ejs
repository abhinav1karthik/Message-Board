<!DOCTYPE html>
<html>
  <head>
    <title>New Message</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        padding: 2rem;
        background: #f2f2f2;
      }
      form {
        background: #fff;
        padding: 1.5rem;
        border-radius: 5px;
        max-width: 400px;
        margin: auto;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }
      label {
        display: block;
        margin-top: 1rem;
      }
      input,
      textarea,
      button {
        width: 100%;
        padding: 0.5rem;
        margin-top: 0.3rem;
        border-radius: 4px;
        border: 1px solid #ccc;
      }
      button {
        margin-top: 1.2rem;
        background-color: #28a745;
        color: white;
        border: none;
        cursor: pointer;
      }
      button:hover {
        background-color: #218838;
      }
    </style>
  </head>
  <body>
    <h1 style="text-align: center">Add a New Message</h1>

    <form method="POST" action="/new">
      <label for="user">Your Name:</label>
      <input type="text" id="user" name="user" required />

      <label for="text">Your Message:</label>
      <textarea id="text" name="text" rows="4" required></textarea>

      <button type="submit">Send Message</button>
    </form>
  </body>
</html>
